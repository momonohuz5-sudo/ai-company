# ローカルで手元検証したい場合のみ .env としてコピーして使う（.env自体はコミットしない）。
# 実運用（毎朝のRoutine）では、この環境の「環境変数」設定に同じ変数名で登録する。
# GOOGLE_DRIVE_CREDENTIALS_JSON にはサービスアカウントのJSONファイルの中身をそのまま1行で入れる。

GEMINI_API_KEY=
GROK_API_KEY=
RETOUCH_API_KEY=
GOOGLE_DRIVE_CREDENTIALS_JSON=
DRIVE_FOLDER_ID=
