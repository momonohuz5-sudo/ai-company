# ローカルで手元検証したい場合のみ .env としてコピーして使う（.env自体はコミットしない）。
# 実運用（毎朝のRoutine）では、この環境の「環境変数」設定に同じ変数名で登録する。
# 保存先は当面Googleドライブ等ではなく、このリポジトリ内(output/)に保存してgit pushする。

GEMINI_API_KEY=
GROK_API_KEY=
RETOUCH_API_KEY=
